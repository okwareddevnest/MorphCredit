{
  "name": "morphcredit",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "contracts",
    "scoring",
    "apps/borrower-pwa",
    "apps/merchant-demo",
    "packages/merchant-sdk"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test"
  }
}
